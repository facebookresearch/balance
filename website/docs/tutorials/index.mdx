---
title: Tutorials and notebooks Overview
sidebar_position: 1
---

The tutorials here present various examples of applying balance end-to-end. Each tutorial is linked to a Jupyter notebook you can download and run from your own environment.

**Requirements**: You will need a [Jupyter installation](https://jupyter.org/) to run these notebooks yourselves. We also assume you have the [balance pkg](/docs/docs/overview) installed.

If you are new to balance, we suggest getting started with the [balance Quickstart](./quickstart) tutorial.

:::tip New: Survey-weighted Difference-in-Differences

The new [**Survey-weighted DiD with diff-diff**](https://import-balance.org/docs/tutorials/balance_diff_diff_brfss/)
tutorial walks through the full workflow combining `balance` (IPW
reweighting against a target frame) with `diff-diff`
(Callaway-Sant'Anna doubly-robust DiD plus HonestDiD sensitivity) on a
BRFSS-shaped public-health panel. It is the recommended starting point
for any survey-based policy or campaign evaluation. Install the optional
extra with `pip install balance[did]`.

:::

{/*
  NOTE: the `:::tip` link above intentionally uses an absolute
  import-balance.org URL rather than the conventional Docusaurus
  relative `./balance_diff_diff_brfss` path. The notebook file ships
  in a separate diff in this stack (the survey-weighted DiD tutorial
  notebook), and Docusaurus's `onBrokenLinks: 'throw'` guard would
  fail the per-diff ephemeral build (e.g.
  `/eph/D<N>/V1/balance/docs/tutorials/balance_diff_diff_brfss/`)
  before the tutorial-file diff has landed. Absolute external URLs
  bypass the broken-link check; once both diffs are landed and the
  site rebuilds, the URL resolves correctly.
*/}

## Tutorials list (more tutorials to be added soon):
1. [**quickstart**](./quickstart) - this is based on a simulated data and presents the simple end-to-end workflow of balance package with default arguments. It demonstrates the process from reading the data, through understanding the biases in the sample, producing weights, evaluating the results and producing the population estimations.
2. [**quickstart_cbps**](./quickstart_cbps) - like the [**quickstart**](./quickstart) tutorial, but shows how to use the CBPS algorithm and compares the results to IPW (logistic regression with LASSO).
3. [**quickstart_rake**](./quickstart_rake) - like the [**quickstart**](./quickstart) tutorial, but shows how to use the rake (raking) algorithm and compares the results to IPW (logistic regression with LASSO).
4. [**quickstart_poststratify**](./quickstart_poststratify) - focuses on post-stratification, highlighting how to match either a single marginal distribution or the full joint distribution of two covariates.
5. [**balance_transformations_and_formulas**](./balance_transformations_and_formulas) - This tutorial showcases ways in which transformations, formulas and penalty can be included in your pre-processing of the covariates before adjusting for them.
6. [**comparing_cbps_in_r_vs_python_using_sim_data**](./comparing_cbps_in_r_vs_python_using_sim_data) - This notebook compares the results of running CBPS in R and Python. In R using the `BCPS` package, and in Python using the `balance` package. The results are almost identical.
7. [**CLI tutorial**](./balance_cli_tutorial) - Walks through the balance command-line interface workflow, including building an input dataset, running the CLI, and reviewing diagnostics.
8. [**balance_ascii_plots**](./balance_ascii_plots) - Demonstrates how to use ASCII plots for visualizing covariate distributions and balance diagnostics in a text-based format.
9. [**quickstart_new_api**](./quickstart_new_api) - Like the quickstart tutorial, but demonstrates the new SampleFrame-based API with explicit column roles (covars, outcomes, ignored) for clearer, more predictable data handling.
